---
# Install Claude Code CLI via its official installer script
- name: Install Claude Code via its installer script
  ansible.builtin.include_tasks: _install_script.yml
  vars:
    install_name: Claude Code
    install_url: https://claude.ai/install.sh
    install_binary: "{{ ansible_env.HOME }}/.local/bin/claude"
