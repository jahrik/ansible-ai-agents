---
# Install Codex CLI via its official installer script
- name: Install Codex via its installer script
  ansible.builtin.include_tasks: _install_script.yml
  vars:
    install_name: Codex
    install_url: https://chatgpt.com/codex/install.sh
    install_binary: "{{ ansible_env.HOME }}/.local/bin/codex"
